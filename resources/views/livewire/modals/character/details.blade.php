<div class="text-center">
    <div class="mt-8">
        <img class="mx-auto h-32" src="{{ asset($this->sprite) }}" />
    </div>
    <div class="mt-8 text-xl">{{ $life->name->name }}</div>
    <div class="text-sm">Family: <span class="capitalize">{{ $life->family_type }}</span></div>
</div>
